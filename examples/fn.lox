fun hello(name) {
    print "Hello " + name + "!";
}

fun count(n) {
    if (n > 1) {
        count(n - 1);
    }

    print n;
}

fun abs(x) {
    if (x <= 1) {
        return -x;

    return x;
}

print abs(-3);